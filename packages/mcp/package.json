{
  "name": "@quizfold/mcp",
  "version": "0.1.0",
  "type": "module",
  "exports": "./src/index.ts",
  "scripts": {
    "build": "bun build src/index.ts --outfile dist/index.js",
    "check": "bunx tsc -p tsconfig.json --noEmit",
    "check:test": "bunx tsc -p tsconfig.test.json --noEmit",
    "test": "bunx tsc -p tsconfig.test.json --noEmit && bunx vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.16.15",
    "@hono/mcp": "^0.3.0"
  }
}
