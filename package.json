{
  "name": "quizfold-workspace",
  "private": true,
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build",
    "build:parser-wasm": "bun run packages/parser/scripts/build.ts",
    "check": "turbo check && cargo check --workspace",
    "check:packages": "turbo check",
    "test": "cargo test --workspace"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/node": "^24.0.0",
    "turbo": "^2.9.16",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  }
}
